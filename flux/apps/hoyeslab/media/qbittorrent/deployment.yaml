apiVersion: apps/v1
kind: Deployment
metadata:
  name: qbittorrent
  namespace: media
  labels:
    app.kubernetes.io/name: qbittorrent
    app.kubernetes.io/instance: qbittorrent
spec:
  strategy:
    type: Recreate
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: qbittorrent
      app.kubernetes.io/instance: qbittorrent
  template:
    metadata:
      labels:
        app.kubernetes.io/name: qbittorrent
        app.kubernetes.io/instance: qbittorrent
    spec:
      # Gluetun runs as a native sidecar (restartPolicy: Always in initContainers).
      # It establishes the VPN tunnel before the main containers start.
      # All containers in the pod share the network namespace, so all traffic
      # from qbittorrent and prowlarr goes through the VPN.
      initContainers:
        - name: gluetun
          image: qmcgaw/gluetun:v3.41.1
          restartPolicy: Always
          securityContext:
            capabilities:
              add:
                - NET_ADMIN
          # Clean up stale WireGuard ip rules from previous gluetun restarts.
          # Kubernetes shares ip rules across the pod, so abrupt container exits
          # leave orphaned rules that prevent the tunnel from re-establishing.
          # See: https://github.com/qdm12/gluetun-wiki/blob/main/setup/advanced/kubernetes.md
          lifecycle:
            postStart:
              exec:
                command: ["/bin/sh", "-c", "(ip rule del table 51820; ip -6 rule del table 51820) || true"]
          env:
            - name: TZ
              value: "America/Toronto"
            - name: VPN_SERVICE_PROVIDER
              value: "custom"
            - name: VPN_TYPE
              value: "wireguard"
            - name: VPN_PORT_FORWARDING
              value: "on"
            - name: VPN_PORT_FORWARDING_PROVIDER
              value: "protonvpn"
            # Set forwarded port as the listen port in qbittorrent
            - name: VPN_PORT_FORWARDING_UP_COMMAND
              value: |
                /bin/sh -c 'wget -O- --retry-connrefused --post-data "json={\"listen_port\":{{PORTS}}}" http://127.0.0.1:8200/api/v2/app/setPreferences 2>&1'
            # WireGuard credentials from 1Password
            - name: WIREGUARD_ENDPOINT_IP
              valueFrom:
                secretKeyRef:
                  name: gluetun-qbittorrent
                  key: endpoint_ip
            - name: WIREGUARD_ENDPOINT_PORT
              valueFrom:
                secretKeyRef:
                  name: gluetun-qbittorrent
                  key: endpoint_port
            - name: WIREGUARD_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: gluetun-qbittorrent
                  key: public_key
            - name: WIREGUARD_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: gluetun-qbittorrent
                  key: private_key
            - name: WIREGUARD_ADDRESSES
              valueFrom:
                secretKeyRef:
                  name: gluetun-qbittorrent
                  key: address
            # Allow inbound cluster traffic to qBittorrent and Prowlarr UIs
            - name: FIREWALL_INPUT_PORTS
              value: "8200,9696"
            # Allow outbound traffic to the cluster and local network for
            # NFS, Kubernetes services (sonarr, radarr, etc), and DNS
            - name: FIREWALL_OUTBOUND_SUBNETS
              value: "10.43.0.0/16,10.52.0.0/16,192.168.2.0/24,192.168.20.0/24"
            # Use plaintext DNS upstream — traffic is already encrypted by the
            # VPN tunnel, and DNS-over-TLS (the default) fails due to MTU issues
            - name: DNS_UPSTREAM_RESOLVER_TYPE
              value: "plain"
          resources:
            requests:
              cpu: "500m"
              memory: "256Mi"
            limits:
              memory: "1Gi"
          startupProbe:
            httpGet:
              path: /
              port: 9999
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /
              port: 9999
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 9999
            periodSeconds: 10

      containers:
        - name: qbittorrent
          image: lscr.io/linuxserver/qbittorrent:5.1.4
          ports:
            - name: qbt-http
              containerPort: 8200
              protocol: TCP
          env:
            - name: PUID
              value: "1029"  # service-access user on Graham-DS920 NFS server
            - name: PGID
              value: "100"   # users group on Graham-DS920 NFS server
            - name: UMASK
              value: "0022"
            - name: TZ
              value: "America/Toronto"
            - name: WEBUI_PORT
              value: "8200"
            - name: DOCKER_MODS
              value: "ghcr.io/themepark-dev/theme.park:qbittorrent"
            - name: TP_THEME
              value: "nord"
          volumeMounts:
            - mountPath: /config
              name: qbittorrent-config
            - mountPath: /data/torrents
              name: media
              subPath: torrents
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "2000m"
              memory: "2Gi"

        # Prowlarr is responsilbe for talking to indexers (on behalf of Radarr and Sonarr).
        # It's in the qbittorrent pod so it can share the VPN connection.
        - name: prowlarr
          image: lscr.io/linuxserver/prowlarr:2.3.5
          ports:
            - name: prowlarr-http
              containerPort: 9696
              protocol: TCP
          env:
            - name: PUID
              value: "1029"  # service-access user on Graham-DS920 NFS server
            - name: PGID
              value: "100"   # users group on Graham-DS920 NFS server
            - name: TZ
              value: "America/Toronto"
            - name: DOCKER_MODS
              value: "ghcr.io/themepark-dev/theme.park:prowlarr"
            - name: TP_THEME
              value: "nord"
          volumeMounts:
            - mountPath: /config
              name: prowlarr-config
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"

      volumes:
        - name: qbittorrent-config
          persistentVolumeClaim:
            claimName: qbittorrent-config
        - name: prowlarr-config
          persistentVolumeClaim:
            claimName: prowlarr-config
        - name: media
          nfs:
            server: ds920.hoyes.dev
            path: /volume1/media
