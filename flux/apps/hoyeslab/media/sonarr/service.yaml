apiVersion: v1
kind: Service
metadata:
  name: sonarr
  namespace: media
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8989
      targetPort: http
  selector:
    app.kubernetes.io/name: sonarr
    app.kubernetes.io/instance: sonarr
