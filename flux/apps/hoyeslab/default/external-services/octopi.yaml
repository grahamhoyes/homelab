apiVersion: v1
kind: Service
metadata:
  name: octopi-proxy
  namespace: default
spec:
  ports:
    - port: 80
      name: http
      targetPort: http
      protocol: TCP
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: octopi-proxy
  namespace: default
  labels:
    kubernetes.io/service-name: octopi-proxy
addressType: IPv4
ports:
  - name: http
    protocol: TCP
    port: 80
endpoints:
  - addresses:
      - "192.168.2.6"
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: octopi-proxy
  namespace: default
spec:
  ingressClassName: nginx
  rules:
    - host: octopi.hoyes.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: octopi-proxy
                port:
                  name: http
  tls:
    - hosts:
        - octopi.hoyes.dev
      secretName: hoyes-dev-tls
